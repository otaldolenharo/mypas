#pragma once
int iskeyword(const char *);

enum keywords{
    BEGIN = 10001,
    NOT,
    OR,
    AND,
    DIV,
    MOD,
    PROGRAM,
    PROCEDURE,
    FUNCTION,
    RETURN,
    VAR,
    INTEGER,
    REAL,
    DOUBLE,
    BOOLEAN,    
    IF,
    THEN,
    ELSE,
    WHILE,
    DO,
    REPEAT,
    UNTIL,
    END,
};